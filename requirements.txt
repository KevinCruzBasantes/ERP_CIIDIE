# ERP CIIDIE — dependencias
# Django 5.2 LTS: compatible con el Python 3.11 del servidor Debian 12
# (Django 6 exige Python 3.12+) y con MariaDB 10.6+. Soporte hasta 2028.
Django~=5.2.16
openpyxl~=3.1.5
Pillow~=12.2

# Desarrollo local (Windows + PostgreSQL)
psycopg[binary]~=3.3 ; sys_platform == "win32"

# Servidor (Debian + MariaDB + gunicorn)
mysqlclient~=2.2 ; sys_platform == "linux"
gunicorn~=23.0 ; sys_platform == "linux"
